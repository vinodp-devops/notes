aws session02
=============================================================
Connect to EC2:  ssh -i daws-88s ec2-user@13.221.110.87

sudo su -
# -> super user
pwd

/root -> root user home directory 
everything is command and text in linux

command-name <inputs> <options>

<inputs> <options> -> optional or mandatory based on the command

uname -> print os/kernel information 
uname --help or man uname

CRUD -> create read update delete
ls -> list subdirectories
ls -l -> long formal Alphabetic order.. files/folders who ownes it, size, timestamp
ls -lr -> Reverse 
ls -lt -> latest files on top
ls -ltr -> latest files at bottom

cd -> to Home Directory from anywhere
ls -ltrrhac -> list, timestamp, reverse, reverse, human-readable, show hidden, sort newest first

hidden files/folders start with .
ls -lart -> hidden files and folders

d rwx r-x r-x -> directory
- rwx r-x r-x -> file

touch devops.txt -> create a file
mkdir devops -> create a directory

cat > devops.txt -> Enter the text you want
Press Enter and Ctrl+d

cat <filename> -> read content in the file
 cat >> devops.txt -> appends
> -> redirection, usually overrides the previous text
>> -> appends to the previous text

rm devops.txt -> Remove file
rm sample.txt -> Remove file
rmdir devops/ -> Remove Directory

source -> which file to copy
destination -> destination

cp <source> <destination>
cp aws.txt devops/
cp linux.txt devops/linux-readhat.txt
cp aws_session02.txt aws_session03.txt repos/daws-88s/


mv -> rename or  move the file
mv <source> <destination>
mv aws.txt devops/
mv linux.txt devops/linux-readhat.txt

Rename:
mv aws.txt devops-with-aws.txt

Downloading files or softwares

wget or curl
https://dlcdn.apache.org/tomcat/tomcat-9/v9.0.113/bin/apache-tomcat-9.0.113.tar.gz

https://raw.githubusercontent.com/daws-88s/notes/refs/heads/main/session-02.txt
https://raw.githubusercontent.com/daws-88s/notes/refs/heads/main/session-03.txt

wget is used for downloading packages
curl -> shows the o/p on the screen, it will not download by default
curl is used to hit the API URL's
curl https://raw.githubusercontent.com/daws-88s/notes/refs/heads/main/session-03.txt

grep -> command to search inside the file

grep <word-to-search> <file>
-i -> case insensitive
-v -> non matching (verbose)
-n -> display line numbers
-c -> matching line numbers count

grep user -vnic session-02.txt

grep 'architecture' session-02.txt
grep 'architecture$' session-02.txt
grep '^architecture' session-02.txt

display line number ending with word architecture
$ -> ending character
^ -> starting character

grep '^ssh' session-02.txt

head -> top 10 lines by default
tail -> bottom 10 lines by default

head session-02.txt 
head -n 3 session-02.txt

Get 5 to 15 lines from file
15-5 = 10 -> tail
15        -> head

head -n 15 session-02.txt | tail -n 10

tail -f <file-name> (-f follow the log dynamically shows)
sudo su -
cd /var/log/  -> Linux logs stores here
ls -l
tail -f dnf.log

cut and awk

https://raw.githubusercontent.com/daws-88s/notes/refs/heads/main/session-02.txt

echo Hello World
echo <context> | cut -d "<delimiter>" -f<fragmet-number>
echo https://raw.githubusercontent.com/daws-88s/notes/refs/heads/main/session-02.txt | cut -d '/' -f1

USER INFO FOUND in this location:
==================================
/etc/passwd/

cut -d "/" -f1 <file-name>
cat /etc/passwd
cut -d ":" -f1,3 /etc/passwd -> get user & userid
cut -d ":" -f1-3 /etc/passwd

awk -F "/" '{print $NF}'
echo https://raw.githubusercontent.com/daws-88s/notes/refs/heads/main/session-02.txt | awk -F "/" '{print $NF}'

awk -F ":" '{print $1F, $3F}' /etc/passwd
cat /etc/passwd | awk -F ":" '{print $1F,$3F}'
awk -F ":" '{print $1F, $NF}' /etc/passwd

cp /etc/passwd users
cat users

filter user id with awk
cat users | awk -F ":" '{print $3F}'

filter non system user 
awk -F ":" '$3>=1000 {print $1, $3}' users

filter nologin users (str comparision)
awk -F ":" '$NF == "/sbin/nologin"{print $1, $3}' users
awk -F ":" '$7F == "/sbin/nologin"{print $1, $3}' users

Tomorrow's session
===================
vim editor
user management

cp aws_session02.txt aws_session03.txt repos/daws-88s/

GIT Config to Git Bash:
------------------------

git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"

git config --list

Sigin with code and activate with Password

cp aws_session02.txt aws_session03.txt repos/daws-88s/
git add .
git status
git commit -m "Updated Session03.txt file"
git push origin main